{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run tests and coverage",
            "type": "shell",
            "command": "nox -s tests",
            "group": "test"
        },
        {
            "label": "Run full PGO/BOLT pipeline",
            "type": "shell",
            "command": "nox -s full_pipeline",
            "group": "test"
        }
    ]
}