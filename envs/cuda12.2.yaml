name: cuda12.2
channels:
  - conda-forge
  - nvidia
  - nodefaults
dependencies:
  # Compilers for this version of CUDA
  - gcc_linux-64=12.2
  - gxx_linux-64=12.2

  # Mark correct CUDA version
  - cuda-version=12.2

  # Requested by OpenMM
  - cudatoolkit=12.2

  # Equivalent to cuda-toolkit=12.2, but actually pulls correct versions
  - cuda-cccl=12.2
  - cuda-command-line-tools=12.2
  - cuda-compiler=12.2
  - cuda-cudart=12.2
  - cuda-cudart-dev=12.2
  - cuda-cuobjdump=12.2
  - cuda-cupti=12.2
  - cuda-cuxxfilt=12.2
  - cuda-driver-dev=12.2
  - cuda-gdb=12.2
  - cuda-libraries=12.2
  - cuda-libraries-dev=12.2
  - cuda-nsight=12.2
  - cuda-nvcc=12.2
  - cuda-nvdisasm=12.2
  - cuda-nvml-dev=12.2
  - cuda-nvprof=12.2
  - cuda-nvprune=12.2
  - cuda-nvrtc=12.2
  - cuda-nvrtc-dev=12.2
  - cuda-nvtx=12.2
  - cuda-nvvp=12.2
  - cuda-opencl=12.2
  - cuda-opencl-dev=12.2
  - cuda-profiler-api=12.2
  - cuda-samples=12.2
  - cuda-sanitizer-api=12.2
  - cuda-thrust=12.2
  - cuda-tools=12.2
  - cuda-version=12.2
  - cuda-visual-tools=12.2

# These variables are set on environment activation (i.e. after `conda activate XXX`)
# $CONDA_PREFIX resolves to the environment's root directory at that point,
# so these paths correctly point to the nvcc compiler and the GCC C++ host compiler
# bundled in this environment. OpenMM requires both to be explicitly set when
# building/running with CUDA support.
variables:
  OPENMM_CUDA_COMPILER: $CONDA_PREFIX/bin/nvcc
  CUDA_HOST_COMPILER: $CONDA_PREFIX/bin/x86_64-conda-linux-gnu-g++
