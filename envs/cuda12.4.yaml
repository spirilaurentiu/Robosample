name: cuda12.4
channels:
  - conda-forge
  - nvidia
  - nodefaults
dependencies:
  # Compilers for this version of CUDA
  - gcc_linux-64=13.2
  - gxx_linux-64=13.2

  # Mark correct CUDA version
  - cuda-version=12.4

  # Requested by OpenMM
  - cudatoolkit=12.4

  # Equivalent to cuda-toolkit=12.4, but actually pulls correct versions
  - cuda-cccl=12.4
  - cuda-command-line-tools=12.4
  - cuda-compiler=12.4
  - cuda-cudart=12.4
  - cuda-cudart-dev=12.4
  - cuda-cuobjdump=12.4
  - cuda-cupti=12.4
  - cuda-cuxxfilt=12.4
  - cuda-driver-dev=12.4
  - cuda-gdb=12.4
  - cuda-libraries=12.4
  - cuda-libraries-dev=12.4
  - cuda-nsight=12.4
  - cuda-nvcc=12.4
  - cuda-nvdisasm=12.4
  - cuda-nvml-dev=12.4
  - cuda-nvprof=12.4
  - cuda-nvprune=12.4
  - cuda-nvrtc=12.4
  - cuda-nvrtc-dev=12.4
  - cuda-nvtx=12.4
  - cuda-nvvp=12.4
  - cuda-opencl=12.4
  - cuda-opencl-dev=12.4
  - cuda-profiler-api=12.4
  - cuda-samples=12.4
  - cuda-sanitizer-api=12.4
  - cuda-thrust=12.4
  - cuda-tools=12.4
  - cuda-version=12.4
  - cuda-visual-tools=12.4

# These variables are set on environment activation (i.e. after `conda activate XXX`)
# $CONDA_PREFIX resolves to the environment's root directory at that point,
# so these paths correctly point to the nvcc compiler and the GCC C++ host compiler
# bundled in this environment. OpenMM requires both to be explicitly set when
# building/running with CUDA support.
variables:
  OPENMM_CUDA_COMPILER: $CONDA_PREFIX/bin/nvcc
  CUDA_HOST_COMPILER: $CONDA_PREFIX/bin/x86_64-conda-linux-gnu-g++
