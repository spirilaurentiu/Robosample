name: cuda12.6
channels:
  - conda-forge
  - nvidia
  - nodefaults
dependencies:
  # Compilers for this version of CUDA
  - gcc_linux-64=13.2
  - gxx_linux-64=13.2

  # Mark correct CUDA version
  - cuda-version=12.6

  # Requested by OpenMM
  - cudatoolkit=12.6

  # Equivalent to cuda-toolkit=12.6, but actually pulls correct versions
  - cuda-cccl=12.6
  - cuda-command-line-tools=12.6
  - cuda-compiler=12.6
  - cuda-cudart=12.6
  - cuda-cudart-dev=12.6
  - cuda-cuobjdump=12.6
  - cuda-cupti=12.6
  - cuda-cuxxfilt=12.6
  - cuda-driver-dev=12.6
  - cuda-gdb=12.6
  - cuda-libraries=12.6
  - cuda-libraries-dev=12.6
  - cuda-nsight=12.6
  - cuda-nvcc=12.6
  - cuda-nvdisasm=12.6
  - cuda-nvml-dev=12.6
  - cuda-nvprof=12.6
  - cuda-nvprune=12.6
  - cuda-nvrtc=12.6
  - cuda-nvrtc-dev=12.6
  - cuda-nvtx=12.6
  - cuda-nvvp=12.6
  - cuda-opencl=12.6
  - cuda-opencl-dev=12.6
  - cuda-profiler-api=12.6
  - cuda-samples=12.6
  - cuda-sanitizer-api=12.6
  - cuda-thrust=12.6
  - cuda-tools=12.6
  - cuda-version=12.6
  - cuda-visual-tools=12.6

# These variables are set on environment activation (i.e. after `conda activate XXX`)
# $CONDA_PREFIX resolves to the environment's root directory at that point,
# so these paths correctly point to the nvcc compiler and the GCC C++ host compiler
# bundled in this environment. OpenMM requires both to be explicitly set when
# building/running with CUDA support.
variables:
  OPENMM_CUDA_COMPILER: $CONDA_PREFIX/bin/nvcc
  CUDA_HOST_COMPILER: $CONDA_PREFIX/bin/x86_64-conda-linux-gnu-g++
