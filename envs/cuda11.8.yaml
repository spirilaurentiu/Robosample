name: cuda11.8
channels:
  - conda-forge
  - nvidia
  - nodefaults
dependencies:
  # Compilers for this version of CUDA
  - gcc_linux-64=11
  - gxx_linux-64=11

  # Mark correct CUDA version
  - cuda-version=11.8

  # Requested by OpenMM
  - cudatoolkit=11.8

  # Equivalent to cuda-toolkit=11.8, but actually pulls correct versions
  - cuda-cccl=11.8
  - cuda-command-line-tools=11.8
  - cuda-compiler=11.8
  - cuda-cudart=11.8
  - cuda-cudart-dev=11.8
  - cuda-cuobjdump=11.8
  - cuda-cupti=11.8
  - cuda-cuxxfilt=11.8
  - cuda-driver-dev=11.8
  - cuda-gdb=11.8
  - cuda-libraries=11.8
  - cuda-libraries-dev=11.8
  - cuda-nsight=11.8
  - cuda-nvcc=11.8
  - cuda-nvdisasm=11.8
  - cuda-nvml-dev=11.8
  - cuda-nvprof=11.8
  - cuda-nvprune=11.8
  - cuda-nvrtc=11.8
  - cuda-nvrtc-dev=11.8
  - cuda-nvtx=11.8
  - cuda-nvvp=11.8
  - cuda-opencl=11.8
  - cuda-opencl-dev=11.8
  - cuda-profiler-api=11.8
  - cuda-samples=11.8
  - cuda-sanitizer-api=11.8
  - cuda-thrust=11.8
  - cuda-tools=11.8
  - cuda-version=11.8
  - cuda-visual-tools=11.8

# These variables are set on environment activation (i.e. after `conda activate XXX`)
# $CONDA_PREFIX resolves to the environment's root directory at that point,
# so these paths correctly point to the nvcc compiler and the GCC C++ host compiler
# bundled in this environment. OpenMM requires both to be explicitly set when
# building/running with CUDA support.
variables:
  OPENMM_CUDA_COMPILER: $CONDA_PREFIX/bin/nvcc
  CUDA_HOST_COMPILER: $CONDA_PREFIX/bin/x86_64-conda-linux-gnu-g++
