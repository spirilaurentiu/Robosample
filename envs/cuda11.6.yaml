name: cuda11.6
channels:
  - conda-forge
  - nvidia
  - nodefaults
dependencies:
  # Compilers for this version of CUDA
  - gcc_linux-64=11
  - gxx_linux-64=11

  # Mark correct CUDA version
  - cuda-version=11.6

  # Requested by OpenMM
  - cudatoolkit=11.6

  # Equivalent to cuda-toolkit=11.6, but actually pulls correct versions
  - cuda-cccl=11.6
  - cuda-command-line-tools=11.6
  - cuda-compiler=11.6
  - cuda-cudart=11.6
  - cuda-cudart-dev=11.6
  - cuda-cuobjdump=11.6
  - cuda-cupti=11.6
  - cuda-cuxxfilt=11.6
  - cuda-driver-dev=11.6
  - cuda-gdb=11.6
  - cuda-libraries=11.6
  - cuda-libraries-dev=11.6
  - cuda-nsight=11.6
  - cuda-nvcc=11.6
  - cuda-nvdisasm=11.6
  - cuda-nvml-dev=11.6
  - cuda-nvprof=11.6
  - cuda-nvprune=11.6
  - cuda-nvrtc=11.6
  - cuda-nvrtc-dev=11.6
  - cuda-nvtx=11.6
  - cuda-nvvp=11.6
  - cuda-opencl=11.6
  - cuda-opencl-dev=11.6
  - cuda-profiler-api=11.6
  - cuda-samples=11.6
  - cuda-sanitizer-api=11.6
  - cuda-thrust=11.6
  - cuda-tools=11.6
  - cuda-version=11.6
  - cuda-visual-tools=11.6

# These variables are set on environment activation (i.e. after `conda activate XXX`)
# $CONDA_PREFIX resolves to the environment's root directory at that point,
# so these paths correctly point to the nvcc compiler and the GCC C++ host compiler
# bundled in this environment. OpenMM requires both to be explicitly set when
# building/running with CUDA support.
variables:
  OPENMM_CUDA_COMPILER: $CONDA_PREFIX/bin/nvcc
  CUDA_HOST_COMPILER: $CONDA_PREFIX/bin/x86_64-conda-linux-gnu-g++
