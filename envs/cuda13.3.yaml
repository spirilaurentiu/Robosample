name: cuda13.3
channels:
  - conda-forge
  - nvidia
  - nodefaults
dependencies:
  # Compilers for this version of CUDA
  - gcc_linux-64=15
  - gxx_linux-64=15

  # Mark correct CUDA version
  - cuda-version=13.3

  # Requested by OpenMM
  - cudatoolkit=13.3

  # Equivalent to cuda-toolkit=13.3, but actually pulls correct versions
  - cuda-cccl=13.3
  - cuda-command-line-tools=13.3
  - cuda-compiler=13.3
  - cuda-cudart=13.3
  - cuda-cudart-dev=13.3
  - cuda-cuobjdump=13.3
  - cuda-cupti=13.3
  - cuda-cuxxfilt=13.3
  - cuda-driver-dev=13.3
  - cuda-gdb=13.3
  - cuda-libraries=13.3
  - cuda-libraries-dev=13.3
  - cuda-nsight=13.3
  - cuda-nvcc=13.3
  - cuda-nvdisasm=13.3
  - cuda-nvml-dev=13.3
  - cuda-nvprof=13.3
  - cuda-nvprune=13.3
  - cuda-nvrtc=13.3
  - cuda-nvrtc-dev=13.3
  - cuda-nvtx=13.3
  - cuda-nvvp=13.3
  - cuda-opencl=13.3
  - cuda-opencl-dev=13.3
  - cuda-profiler-api=13.3
  - cuda-samples=13.3
  - cuda-sanitizer-api=13.3
  - cuda-thrust=13.3
  - cuda-tools=13.3
  - cuda-version=13.3
  - cuda-visual-tools=13.3

# These variables are set on environment activation (i.e. after `conda activate XXX`)
# $CONDA_PREFIX resolves to the environment's root directory at that point,
# so these paths correctly point to the nvcc compiler and the GCC C++ host compiler
# bundled in this environment. OpenMM requires both to be explicitly set when
# building/running with CUDA support.
variables:
  OPENMM_CUDA_COMPILER: $CONDA_PREFIX/bin/nvcc
  CUDA_HOST_COMPILER: $CONDA_PREFIX/bin/x86_64-conda-linux-gnu-g++
