name: cuda11.7
channels:
  - conda-forge
  - nvidia
  - nodefaults
dependencies:
  # Compilers for this version of CUDA
  - gcc_linux-64=11
  - gxx_linux-64=11

  # Mark correct CUDA version
  - cuda-version=11.7

  # Requested by OpenMM
  - cudatoolkit=11.7

  # Equivalent to cuda-toolkit=11.7, but actually pulls correct versions
  - cuda-cccl=11.7
  - cuda-command-line-tools=11.7
  - cuda-compiler=11.7
  - cuda-cudart=11.7
  - cuda-cudart-dev=11.7
  - cuda-cuobjdump=11.7
  - cuda-cupti=11.7
  - cuda-cuxxfilt=11.7
  - cuda-driver-dev=11.7
  - cuda-gdb=11.7
  - cuda-libraries=11.7
  - cuda-libraries-dev=11.7
  - cuda-nsight=11.7
  - cuda-nvcc=11.7
  - cuda-nvdisasm=11.7
  - cuda-nvml-dev=11.7
  - cuda-nvprof=11.7
  - cuda-nvprune=11.7
  - cuda-nvrtc=11.7
  - cuda-nvrtc-dev=11.7
  - cuda-nvtx=11.7
  - cuda-nvvp=11.7
  - cuda-opencl=11.7
  - cuda-opencl-dev=11.7
  - cuda-profiler-api=11.7
  - cuda-samples=11.7
  - cuda-sanitizer-api=11.7
  - cuda-thrust=11.7
  - cuda-tools=11.7
  - cuda-version=11.7
  - cuda-visual-tools=11.7

# These variables are set on environment activation (i.e. after `conda activate XXX`)
# $CONDA_PREFIX resolves to the environment's root directory at that point,
# so these paths correctly point to the nvcc compiler and the GCC C++ host compiler
# bundled in this environment. OpenMM requires both to be explicitly set when
# building/running with CUDA support.
variables:
  OPENMM_CUDA_COMPILER: $CONDA_PREFIX/bin/nvcc
  CUDA_HOST_COMPILER: $CONDA_PREFIX/bin/x86_64-conda-linux-gnu-g++
