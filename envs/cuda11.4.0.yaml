name: cuda11.4.0
channels:
  - conda-forge
  - nvidia
  - nodefaults
dependencies:
  # Compilers for this version of CUDA
  - gcc_linux-64=10
  - gxx_linux-64=10

  # Mark correct CUDA version
  - cuda-version=11.4.0

  # Requested by OpenMM
  - cudatoolkit=11.4.0

  # Equivalent to cuda-toolkit=11.4.0, but actually pulls correct versions
  - cuda-cccl=11.4.0
  - cuda-command-line-tools=11.4.0
  - cuda-compiler=11.4.0
  - cuda-cudart=11.4.0
  - cuda-cudart-dev=11.4.0
  - cuda-cuobjdump=11.4.0
  - cuda-cupti=11.4.0
  - cuda-cuxxfilt=11.4.0
  - cuda-driver-dev=11.4.0
  - cuda-gdb=11.4.0
  - cuda-libraries=11.4.0
  - cuda-libraries-dev=11.4.0
  - cuda-nsight=11.4.0
  - cuda-nvcc=11.4.0
  - cuda-nvdisasm=11.4.0
  - cuda-nvml-dev=11.4.0
  - cuda-nvprof=11.4.0
  - cuda-nvprune=11.4.0
  - cuda-nvrtc=11.4.0
  - cuda-nvrtc-dev=11.4.0
  - cuda-nvtx=11.4.0
  - cuda-nvvp=11.4.0
  - cuda-opencl=11.4.0
  - cuda-opencl-dev=11.4.0
  - cuda-profiler-api=11.4.0
  - cuda-samples=11.4.0
  - cuda-sanitizer-api=11.4.0
  - cuda-thrust=11.4.0
  - cuda-tools=11.4.0
  - cuda-version=11.4.0
  - cuda-visual-tools=11.4.0

# These variables are set on environment activation (i.e. after `conda activate XXX`)
# $CONDA_PREFIX resolves to the environment's root directory at that point,
# so these paths correctly point to the nvcc compiler and the GCC C++ host compiler
# bundled in this environment. OpenMM requires both to be explicitly set when
# building/running with CUDA support.
variables:
  OPENMM_CUDA_COMPILER: $CONDA_PREFIX/bin/nvcc
  CUDA_HOST_COMPILER: $CONDA_PREFIX/bin/x86_64-conda-linux-gnu-g++
