name: cuda12.9
channels:
  - conda-forge
  - nvidia
  - nodefaults
dependencies:
  # Compilers for this version of CUDA
  - gcc_linux-64=14
  - gxx_linux-64=14

  # Mark correct CUDA version
  - cuda-version=12.9

  # Requested by OpenMM
  - cudatoolkit=12.9

  # Equivalent to cuda-toolkit=12.9, but actually pulls correct versions
  - cuda-cccl=12.9
  - cuda-command-line-tools=12.9
  - cuda-compiler=12.9
  - cuda-cudart=12.9
  - cuda-cudart-dev=12.9
  - cuda-cuobjdump=12.9
  - cuda-cupti=12.9
  - cuda-cuxxfilt=12.9
  - cuda-driver-dev=12.9
  - cuda-gdb=12.9
  - cuda-libraries=12.9
  - cuda-libraries-dev=12.9
  - cuda-nsight=12.9
  - cuda-nvcc=12.9
  - cuda-nvdisasm=12.9
  - cuda-nvml-dev=12.9
  - cuda-nvprof=12.9
  - cuda-nvprune=12.9
  - cuda-nvrtc=12.9
  - cuda-nvrtc-dev=12.9
  - cuda-nvtx=12.9
  - cuda-nvvp=12.9
  - cuda-opencl=12.9
  - cuda-opencl-dev=12.9
  - cuda-profiler-api=12.9
  - cuda-samples=12.9
  - cuda-sanitizer-api=12.9
  - cuda-thrust=12.9
  - cuda-tools=12.9
  - cuda-version=12.9
  - cuda-visual-tools=12.9

# These variables are set on environment activation (i.e. after `conda activate XXX`)
# $CONDA_PREFIX resolves to the environment's root directory at that point,
# so these paths correctly point to the nvcc compiler and the GCC C++ host compiler
# bundled in this environment. OpenMM requires both to be explicitly set when
# building/running with CUDA support.
variables:
  OPENMM_CUDA_COMPILER: $CONDA_PREFIX/bin/nvcc
  CUDA_HOST_COMPILER: $CONDA_PREFIX/bin/x86_64-conda-linux-gnu-g++
