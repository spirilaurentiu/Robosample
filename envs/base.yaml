name: robo_base
channels:
  - conda-forge
  - nodefaults
dependencies:
  # Build tools
  - cmake
  - ninja
  - pkg-config
  - gcc_linux-64
  - gxx_linux-64

  # Python
  - python=3.12
  - pip

  # Coding tools
  - gtest

  # Hardware acceleration requested and installed by OpenMM
  # This happens regardless of the underlying hardware
  - ocl-icd
  - ocl-icd-system
  - opencl-headers
  - clinfo
  - rocm-core

  # OpenMP support for GNU and LLVM compilers
  - libgomp

  # Bioinformatics
  - openmm
  - openmmtools
  - parmed
  - mdtraj
  - mdanalysis
  - salilab::modeller
  - rdkit
  - pymol-open-source

  # AMBER tools
  - dacase::ambertools-dac=26
  - netcdf4=*=mpi_openmpi_py312*
  - netcdf-fortran=*=mpi_openmpi*

  # Scientific computing and data analysis
  - numpy
  - scipy
  - pyerfa=*=py312*
  - astropy=*=py312*
  - networkx
  - tqdm
  - hypothesis
  - pandas
  - matplotlib
  - seaborn

  # Pip packages
  - pip:
    - conda-merge
    - pybind11-stubgen
    - pytest
    - pytest-xdist
    - pytest-cov
    - conda-merge
    - gcovr
    - coverage
    - anybadge
    - nox
    - pre-commit
    - clang-tidy
    - clang-format
    - cmake-format
    - scikit-build-core
    - python-louvain
    - openbabel-wheel
    - rcsbsearchapi
    - pdb2pqr
