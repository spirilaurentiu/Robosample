name: robo_py312
channels:
  - conda-forge
  - nodefaults
config:
  channel_priority: flexible
dependencies:
  # Build tools
  - cmake
  - ninja
  - pkg-config
  - conda-forge::gcc_linux-64
  - conda-forge::gxx_linux-64

  # Python
  - python
  - pip

  # Coding tools
  - gtest

  # Hardware acceleration (CUDA is installed separately)
  - ocl-icd
  - ocl-icd-system
  - opencl-headers
  - clinfo
  - rocm-core

  # OpenMP support for GNU and LLVM compilers
  - libgomp

  # Bioinformatics
  - openmm
  - parmed
  - mdtraj
  - mdanalysis

  # Scientific computing and data analysis
  - numpy
  - scipy
  - astropy
  - networkx
  - tqdm
  - hypothesis

  # Pip packages
  - pip:
    - pybind11-stubgen
    - pytest
    - pytest-xdist
    - pytest-cov
    - gcovr
    - coverage
    - anybadge
    - nox
    - pre-commit
    - clang-tidy
    - clang-format
    - cmake-format
    - scikit-build-core
    - python-louvain
