name: cuda12.0
channels:
  - conda-forge
  - nvidia
  - nodefaults
dependencies:
  # Compilers for this version of CUDA
  - gcc_linux-64=12.1
  - gxx_linux-64=12.1

  # Mark correct CUDA version
  - cuda-version=12.0

  # Requested by OpenMM
  - cudatoolkit=12.0

  # Equivalent to cuda-toolkit=12.0, but actually pulls correct versions
  - cuda-cccl=12.0
  - cuda-command-line-tools=12.0
  - cuda-compiler=12.0
  - cuda-cudart=12.0
  - cuda-cudart-dev=12.0
  - cuda-cuobjdump=12.0
  - cuda-cupti=12.0
  - cuda-cuxxfilt=12.0
  - cuda-driver-dev=12.0
  - cuda-gdb=12.0
  - cuda-libraries=12.0
  - cuda-libraries-dev=12.0
  - cuda-nsight=12.0
  - cuda-nvcc=12.0
  - cuda-nvdisasm=12.0
  - cuda-nvml-dev=12.0
  - cuda-nvprof=12.0
  - cuda-nvprune=12.0
  - cuda-nvrtc=12.0
  - cuda-nvrtc-dev=12.0
  - cuda-nvtx=12.0
  - cuda-nvvp=12.0
  - cuda-opencl=12.0
  - cuda-opencl-dev=12.0
  - cuda-profiler-api=12.0
  - cuda-samples=12.0
  - cuda-sanitizer-api=12.0
  - cuda-thrust=12.0
  - cuda-tools=12.0
  - cuda-version=12.0
  - cuda-visual-tools=12.0

# These variables are set on environment activation (i.e. after `conda activate XXX`)
# $CONDA_PREFIX resolves to the environment's root directory at that point,
# so these paths correctly point to the nvcc compiler and the GCC C++ host compiler
# bundled in this environment. OpenMM requires both to be explicitly set when
# building/running with CUDA support.
variables:
  OPENMM_CUDA_COMPILER: $CONDA_PREFIX/bin/nvcc
  CUDA_HOST_COMPILER: $CONDA_PREFIX/bin/x86_64-conda-linux-gnu-g++
