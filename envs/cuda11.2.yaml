name: cuda11.2
channels:
  - conda-forge
  - nvidia
  - nodefaults
dependencies:
  # Compilers for this version of CUDA
  - gcc_linux-64=10
  - gxx_linux-64=10

  # Mark correct CUDA version
  - cuda-version=11.2

  # Requested by OpenMM
  - cudatoolkit=11.2

  # Equivalent to cuda-toolkit=11.2, but actually pulls correct versions
  - cuda-command-line-tools=11.2
  - cuda-compiler=11.2
  - cuda-cudart=11.2
  - cuda-cuobjdump=11.2
  - cuda-cupti=11.2
  - cuda-cuxxfilt=11.2
  - cuda-gdb=11.2
  - cuda-libraries=11.2
  - cuda-libraries-dev=11.2
  - cuda-memcheck=11.2
  - cuda-nvcc=11.2
  - cuda-nvdisasm=11.2
  - cuda-nvprof=11.2
  - cuda-nvprune=11.2
  - cuda-nvrtc=11.2
  - cuda-nvtx=11.2
  - cuda-nvvp=11.2
  - cuda-sanitizer-api=11.2
  - cuda-thrust=11.2
  - cuda-tools=11.2
  - cuda-visual-tools=11.2

# These variables are set on environment activation (i.e. after `conda activate XXX`)
# $CONDA_PREFIX resolves to the environment's root directory at that point,
# so these paths correctly point to the nvcc compiler and the GCC C++ host compiler
# bundled in this environment. OpenMM requires both to be explicitly set when
# building/running with CUDA support.
variables:
  OPENMM_CUDA_COMPILER: $CONDA_PREFIX/bin/nvcc
  CUDA_HOST_COMPILER: $CONDA_PREFIX/bin/x86_64-conda-linux-gnu-g++
